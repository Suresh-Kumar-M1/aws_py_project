day-2 

host web server on ec2 machine

step-1
create 2 ec2 machine for both windows and linux host website on both instance

step-2
 for host website on windows we want to install the IIS 
 
 step -3 
 for host website on linux ew want to install Appeache - "sudo yum install httpd"
 
 for  go to root user "sudo su - root"
 to start Appeache server - "service httpd start"
  for the wesite path - "/var/www/html/
  to use of WInscp we easily copy and paste the folders in the file